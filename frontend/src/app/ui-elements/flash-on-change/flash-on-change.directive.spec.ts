import { FlashOnChangeDirective } from './flash-on-change.directive';

describe('FlashOnChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new FlashOnChangeDirective();
    expect(directive).toBeTruthy();
  });
});
